import { ComponentFixture, TestBed } from '@angular/core/testing';
import { JatetxeaPage } from './jatetxea.page';

describe('JatetxeaPage', () => {
  let component: JatetxeaPage;
  let fixture: ComponentFixture<JatetxeaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(JatetxeaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
